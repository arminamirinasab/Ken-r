<?xml version="1.0" encoding="utf-8"?>
<!-- Declares the locales the app ships. Persian first (primary). -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="fa" />
    <locale android:name="en" />
</locale-config>
